export interface HeroProps {
  className?: string;
}

export interface FeatureItem {
  id: string;
  text: string;
  icon: string;
}

export interface SupportedPlatform {
  id: string;
  name: string;
  icon: string;
}